# Staging Environment Configuration
NODE_ENV=staging
PORT=5000

# Database Configuration (Render will provide these)
# DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD will be set by Render

# Security
CORS_ORIGIN=https://courtpulse-staging.vercel.app
JWT_SECRET=staging_jwt_secret_key_123

# Logging
LOG_LEVEL=info

# External Services (staging keys)
GOOGLE_MAPS_API_KEY=staging_google_maps_key
MAPBOX_ACCESS_TOKEN=staging_mapbox_token
